name: Renovate template config CI

on:
  workflow_dispatch:
  pull_request:
    paths:
      - 'default.json'

jobs:
  build:
    uses: chalharu/workflows/.github/workflows/renovate-ci.yaml@3bfec781162b1745d44899e5b7ea562eeb4107a7 # main
    with:
      target: default.json
