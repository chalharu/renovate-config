name: Renovate template config CI

on:
  workflow_dispatch:
  pull_request:
    paths:
      - 'default.json'

jobs:
  build:
    uses: chalharu/workflows/.github/workflows/renovate-ci.yaml@814eb57dc8122945b7bde65e98563044db14dd0f # main
    with:
      target: default.json
