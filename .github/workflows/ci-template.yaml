name: Renovate template config CI

on:
  workflow_dispatch:
  pull_request:
    paths:
      - 'default.json'

jobs:
  build:
    uses: chalharu/workflows/.github/workflows/renovate-ci.yaml@bb7d80df12b3efd7d5b4f0611beaff68176c0747 # main
    with:
      target: default.json
