name: Renovate template config CI

on:
  workflow_dispatch:
  pull_request:
    paths:
      - 'default.json'

jobs:
  build:
    uses: chalharu/workflows/.github/workflows/renovate-ci.yaml@8639bd74d9c392e161e67d10d32bb447b8d35095 # main
    with:
      target: default.json
