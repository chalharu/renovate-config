name: Renovate template config CI

on:
  workflow_dispatch:
  pull_request:
    paths:
      - 'default.json'

jobs:
  build:
    uses: chalharu/workflows/.github/workflows/renovate-ci.yaml@b38333d680878a2156ff25b255cf7174c9a3a065 # main
    with:
      target: default.json
