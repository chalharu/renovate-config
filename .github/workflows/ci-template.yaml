name: Renovate template config CI

on:
  workflow_dispatch:
  pull_request:
    paths:
      - 'default.json'

jobs:
  build:
    uses: chalharu/workflows/.github/workflows/renovate-ci.yaml@9eb427bb8af6ac98962ecb1502bdd927bf97c99f # main
    with:
      target: default.json
